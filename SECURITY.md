# Security

Do not report exposed credentials in a public issue. Revoke the credential and
contact the repository owner privately. This project intentionally excludes
training data, test labels, per-row predictions, model weights, SSH settings,
and environment files.
